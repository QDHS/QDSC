<%= form_for(@user,:url=>"/users/new") do |f| %>
	<div class="field">
		<%= f.label :email %><br>
		<%= f.text_field :email %>@qibaodwight.org
	</div>
	<div class="field">
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
